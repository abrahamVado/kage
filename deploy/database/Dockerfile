# //1.- Extend the official MariaDB image to keep compatibility with standard tooling.
FROM mariadb:11.2

# //2.- Set a default command that runs the database server (retained from the base image).
CMD ["mysqld"]
